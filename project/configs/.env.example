# Шаблон переменных окружения.
# Скопируй в .env и заполни — реальный .env НЕ коммитить!

CONFIG_PATH=configs/config.yaml
MODEL_PATH=data/model_artifacts.pkl
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8000
LOG_LEVEL=INFO
